angular
    .module('altairApp')
    .controller('feeStructViewCtrl', [
        '$scope',
        '$window',
        '$timeout',
        function ($scope,$window,$timeout) {

        }
    ]);